import 'package:flutter/widgets.dart';

class MaterialHome extends StatelessWidget {
  const MaterialHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Material Home Screen'),
    );
  }
}